[project]
name = "artistic-ai-exp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "transformers[torch]>=5.6",
    "openai>=2.8",
    "rich>=14.2",
    "jinja2>=3.1",
    "trl[peft]>=1.3",
    "numpy>=2.4",
]

[project.optional-dependencies]
dev = [
    "ruff",
]
