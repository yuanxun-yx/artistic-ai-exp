[project]
name = "artistic-ai-exp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "transformers[torch]>=5.7",
    "openai>=2.33",
    "rich>=14.2",
    "jinja2>=3.1",
    "trl[peft]>=1.3",
]

[project.optional-dependencies]
dev = [
    "ruff",
]
